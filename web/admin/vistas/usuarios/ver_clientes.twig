{% extends 'inicio.twig' %}

{% block titulo 'Clientes' %}

	{% block action %}
			<table class="tablacrud">
					<tr class="trcab" >
						<th colspan="3">
							<a href="{{app.request.basepath}}/usuarios/clientes/add"><input type="button" name="btadd" id="btadd" class="btadd" value="Añadir Registro" /></a>
							{{ form_start(form, {'method': 'POST'}) }}
							{{ form_row(form.Borrar)}}
						</th>
					</tr>
					<tr class="trcab">
						<th>Lista de Clientes</th>
					</tr>
					{% for cliente in clientes %}
						<input type="hidden" name="hidid" value="{{cliente.email}}"/>
						<tr>
							<td colspan="4">
								<table class="bodycrud" rules="all">
									<thead>
										<tr class="trtit">
											<th>
												email
											</th>
											<th>
												nombre
											</th>
											<th>#</th>
										</tr>
									</thead>
									<tbody>
										<tr>
											<td class="perla">
												<a href="{{app.request.basepath}}/usuarios/clientes/cliente/{{cliente.email}}">
												{{cliente.email}}
												</a>
											</td>
											<td class="grey">
												<a href="{{app.request.basepath}}/usuarios/clientes/cliente/{{cliente.email}}">
												{{cliente.nombre}}
												</a>
											</td>
											<td class="perla">
												<input type="checkbox" name="cb_borrar[]" id="cb_borrar[]" value="{{cliente.email}}"/>
											</td>
										</tr>
					{% endfor %}
									</tbody>

								</table>
							</td>
						</tr>
					{{ form_end(form) }}
				</table>
	{% endblock %}