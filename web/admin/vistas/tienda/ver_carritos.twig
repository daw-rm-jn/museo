{% extends 'inicio.twig' %}

{% block titulo 'Carritos' %}

	{% block action %}
			<table class="tablacrud">
					<tr class="trcab" >
						<th colspan="3">
							<a href="{{app.request.basepath}}/tienda/carritos/add"><input type="button" name="btadd" id="btadd" class="btadd" value="Añadir Registro" /></a>
							{{ form_start(form, {'method': 'POST'}) }}
							{{ form_row(form.Borrar)}}
						</th>
					</tr>
					<tr class="trcab">
						<th>Lista de Carritos activos</th>
					</tr>
					{% for carrito in carritos %}
						<input type="hidden" name="hidid" value="{{carrito.idCarrito}}"/>
						<tr>
							<td colspan="4">
								<table class="bodycrud" rules="all">
									<thead>
										<tr class="trtit">
											<th>
												idCarrito
											</th>
											<th>
												email
											</th>
											<th>
												Fecha Expiración
											</th>
											<th>
												#
											</th>
										</tr>
									</thead>
									<tbody>
										<tr>
											<td class="perla">
												<a href="{{app.request.basepath}}/tienda/carritos/carrito/{{carrito.idCarrito}}">
												{{carrito.idCarrito}}
												</a>
											</td>
											<td class="grey">
												<a href="{{app.request.basepath}}/tienda/carritos/carrito/{{carrito.idCarrito}}">
												{{carrito.email}}
												</a>
											</td>
											<td class="perla">
												{{carrito.fechaExpir}}
											</td>
											<td class="grey">
												<input type="checkbox" name="cb_borrar[]" id="cb_borrar[]" value="{{carrito.idCarrito}}"/>
											</td>
										</tr>
					{% endfor %}
									</tbody>

								</table>
							</td>
						</tr>
					{{ form_end(form) }}
				</table>
	{% endblock %}